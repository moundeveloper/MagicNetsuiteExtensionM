{
  "name": "magic-netsuite-sdf-tool",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "SDF deploy companion: creates NetSuite script records + deployments from a JSON spec via the bundled SuiteCloud CLI. Self-scaffolds and reuses an SDF project beside the exe.",
  "main": "src/cli.js",
  "bin": {
    "sdf-deploy": "src/cli.js"
  },
  "scripts": {
    "build": "pkg src/cli.js -t node18-win-x64 --output sdfDeploy.exe",
    "start": "node src/cli.js"
  },
  "dependencies": {
    "@oracle/suitecloud-cli": "3.1.3"
  },
  "pkg": {
    "scripts": "src/*.js"
  }
}
